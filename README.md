# Chromatic Mycelium

A responsive static canvas website for Vercel.

Deploy the folder directly to Vercel. The site has no build-time dependencies; `index.html`, `styles.css`, and `src/main.js` are all that the experience needs.
